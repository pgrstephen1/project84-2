import React, {Components} from 'react';
import {Text, View, StyleSheet} from 'react-native';

export default class PostScreen extends Components{
    render(){
        return(
            <View>
                
            </View>
        );
    }
}